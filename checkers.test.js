Checkers = require("./checkers");

describe('Checkers', () => {
    let checkers;

    beforeEach(() => {
        checkers = new Checkers(null)
    });

    it('should init without issue', () => {

    });
});